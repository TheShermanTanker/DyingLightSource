import "values.mtt"

// EYE ///////////////////////////////////////////////////
sub logo(int e_path = -1)
{
    SetRenderTarget("backbuffer", "color stencil depth");
		Mesh("tri_screen.msh", "logo_3.mat", "std");

	// There must be at least one scene
	Scene("std");
}
