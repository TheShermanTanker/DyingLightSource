// This file is used in ALL respack compiler invokes (for P4, for builds and for everything).
// It is added directly from the ResPackCompiler code.

import "disable_compression.scr"

sub main()
{
	use disable_compression();
}
