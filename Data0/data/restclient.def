// Server
!PLSServer(s)

// Master enable
!EnablePLS(i)

// Individual feature enabling
!EnableMOTDs(i)
!EnablePatches(i)
!EnableEvents(i)
!EnableRewards(i)
!EnableRejectionChecksums(i)

// What release level data to ask for
!ReleaseLevel(i)

// Enable downloading patches for testing
!EnableTestPatching(i)

// Size of buffer for downloading
!DownloadBufferSize(i)

// For testing - get a patch or an event
!TestGetPatch(i)
!TestGetEvent(i)

// For testing - whether to use database connection or local disk
!TestUseLocalFilesInsteadOfServer(i)

!DevServer(s)
!ProdServer(s)
